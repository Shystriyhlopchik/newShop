export enum Variant {
  Primary = 'primary',
  Accent = 'accent',
  Success = 'success',
  Warning = 'warning',
  Default = 'default',
}

export enum Size {
  Default = 'default',
  Large = 'large',
  Small = 'small',
}
